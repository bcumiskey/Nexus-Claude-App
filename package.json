{
  "name": "nexus",
  "version": "1.0.0",
  "private": true,
  "description": "Nexus — Claude Operations Platform",
  "scripts": {
    "setup": "cd server && npm install && cd ../client && npm install",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "dev": "echo 'Run in two terminals: npm run server && npm run client'",
    "start": "cd server && npm start"
  }
}
